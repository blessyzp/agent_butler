# 电子管家（Butler）技术设计文档

一个本地优先、全程加密的个人 AI Agent：多轮对话、任务管理、语音/图片输入、
资源感知的模型调度、按用户画像自适应的智能提醒。本文档记录系统架构、
关键设计决策及其背后的权衡，供后续维护与技术复盘参考。

---

## 1. 系统定位与硬约束

单用户本地部署的"电子管家"：在个人电脑（RTX 3060 12GB / 16GB RAM）上跑
本地大模型，云端仅作为资源不足时的兜底。核心硬约束（决定了几乎所有架构决策）：

> 模型选型会持续迭代（换更大/更小的本地模型、换嵌入模型、换云端供应商），
> 每次迭代前后都不能丢数据、不能破坏已有功能。

由此确立三条设计原则：

1. **文本是真相，向量是缓存** —— 语义检索用的向量索引可以随时从加密文本
   重新生成，因此向量库故障、模型更换都不会导致数据丢失，最多退化到
   "按时间召回"。
2. **schema 有版本** —— 数据库结构变更走只增不删的迁移脚本，任何环境下
   启动都会自动升级到最新版本，且升级前自动快照。
3. **模型是插件** —— 业务代码只认"角色"（chat_large / chat_small /
   chat_cloud / vision / embed），不认具体模型；换模型只改配置文件。

---

## 2. 架构总览

```
                         ┌─────────────────────┐
用户输入 ──────────────▶│      Butler 主控      │◀────── ReminderEngine
(文字/语音/图片)         │  (对话/提取/落库编排)  │        (定时 tick)
                         └──────────┬───────────┘
              ┌───────────┬─────────┼──────────┬────────────┐
              ▼           ▼         ▼          ▼            ▼
       ResourceMonitor  Scheduler  Registry   Memory      Profile
       (VRAM/RAM/CPU)  (压力→角色  (角色→模型  (加密SQLite  (加密JSON
        排除自身进程)   +防抖+     +后端缓存)   +Chroma向量)  画像演化)
                        回退链)        │
                                       ▼
                                 LLMBackend
                              (Ollama / OpenAI兼容)
```

分层职责单一、依赖方向清晰：`Butler` 是唯一的编排者，其余模块互不感知；
`Registry`/`LLMBackend` 把"用什么模型"与"怎么用模型"完全解耦；
`Memory` 把"加密结构化数据"与"向量语义索引"绑定成一个对外原子的整体，
调用方不需要关心 Chroma 是否可用。

前端是零构建的原生 HTML/CSS/JS 单页应用，通过 `FastAPI` + `StaticFiles`
挂载在同一进程里，四个标签页（聊天/任务/设置/状态）均是纯 REST 调用，
没有额外的前端框架依赖。

---

## 3. 核心模块与关键设计

### 3.1 加密层（`crypto.py`）

- **对称加密**：Fernet（AES-128-CBC + HMAC），密钥经 PBKDF2-HMAC-SHA256
  （20 万轮）从用户主密码派生，派生盐固定存于代码中（盐本身不是机密，
  作用是防彩虹表，不是防暴力破解——真正的强度来自轮数和主密码强度）。
- **主密码获取优先级**：环境变量 → 操作系统密钥链（Windows Credential
  Locker，通过 `keyring`）→ 交互式输入并写回密钥链。三级兜底保证既能在
  桌面场景免密启动，也能在自动化/测试场景无交互运行。
- **单例模式**：`Cipher.instance()` 保证一次派生、进程内复用，避免每次
  加解密都重跑一次 PBKDF2（200,000 轮并不便宜）。
- **加密边界**：只加密"内容"字段（任务描述、对话文本、画像 JSON、偏好
  值），不加密结构性元数据（时间戳、状态枚举、优先级数字）——因为后者
  需要被 SQL 直接过滤/排序，加密后无法索引。这是一个显式的隐私 vs
  可查询性权衡：结构化元数据泄露的信息量远小于内容本身。

### 3.2 记忆系统（`memory.py` + `versioning.py`）

**双层存储**：

| 层 | 存储 | 定位 |
|---|---|---|
| 真相源 | 加密 SQLite（WAL 模式） | 唯一权威数据源，任务/画像信号/对话文本全部落在这里，加密后落盘 |
| 向量索引 | ChromaDB（可选） | 派生缓存，用于语义检索；集合名绑定嵌入模型 ID，换模型即换集合 |

**为什么向量库必须"可丢"**：嵌入模型的向量空间和模型强绑定，换模型后旧
向量在语义上失效。若把向量当成真相源，换模型就等于损失历史语义检索
能力。设计上把 `episodes` 表（加密文本）当唯一真相源，向量集合命名为
`episodes__<embed_model_id>`，检测到当前配置的嵌入模型与已记录的不一致时，
自动：**快照备份 → 从真相源逐条重新计算 embedding → 写入新集合 → 校验
条数一致 → 原子切换**，旧集合原样保留，出问题可直接回滚指回旧集合。

**schema 版本化**（`versioning.py`）：用 SQLite 的 `PRAGMA user_version`
记录当前结构版本，`MIGRATIONS` 是一个只增不删的 `(目标版本, SQL列表)`
列表，`ensure_schema()` 每次启动时从当前版本顺序补齐到最新版本，天然
幂等（重复调用不会重复执行已完成的迁移）。新增字段永远是新的迁移条目，
不修改已发布的迁移脚本——这保证了任意历史版本的库都能安全升级到最新版本，
不依赖"从头建库"的路径。

**并发安全**：SQLite 默认 `DELETE` 日志模式下，长事务会阻塞其他连接；
本项目有两个并发写者（API 请求线程 + APScheduler 后台提醒线程），因此
开启 `PRAGMA journal_mode=WAL`（写不阻塞读，多个连接可并发写入不同页）。
这带来一个容易被忽视的一致性陷阱：WAL 模式下最近提交的事务可能还没
"checkpoint"回主 `.db` 文件，如果备份逻辑是裸文件复制（`shutil.copy2`），
会漏掉这部分未回写的数据。修复方式是用 SQLite 官方 `Connection.backup()`
API 做一致性快照，它会正确处理 WAL 日志，而不是绕开数据库层直接碰文件。

### 3.3 资源感知调度（`resource_monitor.py` + `scheduler_model.py`）

**目标**：在同一台游戏本上跑本地大模型，不能和游戏/其他重负载软件抢
显存，也不能因为"检测到游戏进程名"这种脆弱的硬编码方式失效。

**实现**：不识别任何具体进程用途，只做"排除自身、统计其余"的通用扣除——
先枚举管家自身及其子进程/父进程的 PID 集合（`_butler_pids()`），再用
`nvidia-smi --query-compute-apps` 按进程查询显存占用，减去自身占用得到
"其他软件在用多少显存"。这个方案在设计阶段踩过一个坑：部分消费级显卡
驱动下 `nvidia-smi` 按进程查询显存会返回 `[N/A]`（不支持逐进程统计），
若不做兜底，"其他软件占用"会被误判为 0，即使 GPU 已经跑满。修复方案是
双查询：先尝试逐进程查询，检测到全部返回 `[N/A]` 时自动回退为整卡
`memory.used` 作为"其他占用"的近似值——牺牲一点精度（不能精确扣除管家
自身的显存占用）换取"至少不会误判为空闲"的安全边界。

**压力分级与防抖**：VRAM/RAM/CPU 三个维度取最紧张者决定压力等级
（low/medium/high/critical），映射到模型角色（本地大/本地小/云端）。
纯阈值触发在临界点附近会因采样抖动反复切换模型（频繁加载/卸载模型本身
是重操作），因此加入切换冷却窗口（默认 60 秒）：目标角色变化后，若
距上次切换不足冷却时间，暂时维持当前角色不动，等冷却期过后才真正切换。

**可用性回退链**：即便压力评级选中了某个角色，若该角色对应的后端实际
不可用（Ollama 没启动、云端没配密钥），会沿着"本地大 → 本地小 → 云端"
链条依次尝试，全部失败才抛出明确的诊断异常，而不是让请求默默失败。

### 3.4 提醒与监督引擎（`reminder.py`）

不是简单的定时轮询提醒，核心是两个自适应机制：

**提前量自适应**：`提前量 = 默认提前量 × (1 + 2 × 拖延分)`，拖延分来自
用户画像里"该类任务过去按时 vs 逾期完成的比例"。越容易拖延某类任务，
系统提前提醒的时间越长（最多放大 3 倍）；从不拖延的类型维持默认提前量，
不过度打扰。

**逾期追问退避**：这是本项目实测中发现并修复的一个真实缺陷（见第 5
节）——逾期任务的追问必须带指数退避阶梯（1/3/6/12/24 小时），否则
"每 5 分钟检查一次、每次都重发"会在 20 分钟内把当天的提醒配额打满，
既骚扰用户又挤占了真正临期任务的提醒额度。退避状态落在
`tasks.last_reminded_at` 列上，按"已提醒次数"索引到退避阶梯的对应档位。

**每日配额**：从 `events` 事件日志表实时统计当日已发送的提醒数，而非
用内存变量计数——内存计数在服务重启后会清零，等于给了一个"重启一次就
能再打满一轮配额"的漏洞。

### 3.5 用户画像（`profile.py`）

模型中立的结构化画像（语气偏好、活跃时段直方图、按任务类型统计的拖延/
守时次数、长期目标、行为笔记），加密后以 JSON 存盘，与对话记忆是同级
的独立真相源。画像的更新完全由本模块的显式方法驱动（`record_procrastination`
/ `record_reliable` / `absorb_signals`），LLM 只负责"从对话里提取信号"，
不直接写画像——这样画像的演化逻辑始终可审计、可单测，不依赖模型输出的
稳定性。

拖延分计算是 `p / (p + r)`（p=拖延次数，r=按时次数），无数据时给出
中性默认值 0.3，避免"数据不足 = 极端拖延"的误判。这里同样是一处实测
修复过的缺陷：记账时机必须在"任务完成时"按时/逾期二选一记一次，而不能
在"发提醒时"记账——按提醒次数记账会导致同一条任务每被追问一次分数就
上涨一次，一个逾期任务在短时间内被追问多次就能把某个类别的拖延分打到
满分，且因为项目最初没人调用"记守时"的那一半，分数只会单向上涨、永不
回落。

### 3.6 任务优先级与重复任务（本轮新增）

在 schema v3 给 `tasks` 表新增两列：`priority`（0=普通/1=重要/2=紧急，
整数越大排序越靠前）和 `recurrence`（`daily`/`weekly`/`monthly`/`null`）。
设计要点：

- **列表排序**：`ORDER BY priority DESC, due_at IS NULL, due_at`——高
  优先级永远排在前面，同优先级内再按"有截止时间的在前、按时间早晚"排。
- **自动续期而非定时预生成**：重复任务不是"提前生成未来 N 次"，而是
  在 `complete_task()` 里检测到 `recurrence` 非空时，以刚完成任务的
  原定截止时间为基准 + 周期步长，落一条新的 pending 任务，携带同样的
  `recurrence` 规则。这样"未完成的周期任务"永远只有一条在途，不会
  因为服务重启多次而重复生成，也不会在用户忘记完成时无限堆积。
- **对话提取兼容**：LLM 的任务提取 prompt 同步要求输出 `priority`/
  `recurrence` 字段，但两者都做了严格的白名单校验（`_sane_priority`/
  `_sane_recurrence`），模型输出越界或编造值时静默降级为默认值（0 /
  null），不会因为小模型的幻觉写入脏数据。

### 3.7 结构化任务提取（LangChain，本轮重构）

原方案是在 system prompt 里拜托模型"回复后另起一行输出一个 ```json 代码块"，
拿到回复后用正则 `_split_extraction()` 抠出代码块再 `json.loads()`。能跑，
但脆弱：模型只要没有严格遵守格式（多写一句客套话、漏一个反引号），提取就
静默失败退化成空字典；而且没有 schema 级别的类型/取值约束，`priority`/
`recurrence` 越界完全靠事后写的白名单校验补救。

重构为 LangChain 的 `with_structured_output()`：在 `src/extraction.py` 定义
`ChatExtraction`（`reply` + `tasks: list[TaskExtraction]` +
`profile_signals: ProfileSignals`）这个 pydantic schema，`priority` 用
`Literal[0, 1, 2]`、`recurrence` 用 `Literal["daily", "weekly", "monthly"] |
None` 直接在 schema 层面约束取值范围。一次模型调用直接产出这整个结构化
对象，不是"先生成自由文本再解析"的两段式，不增加额外的模型调用延迟。

**技术选型踩坑**：`with_structured_output()` 支持三种 `method`。最初按常规
思路选了 `"function_calling"`（工具调用），但实测在本地 `qwen2.5:7b` 上
不稳定——模型经常直接不触发工具调用，`tool_calls` 返回空列表，退化成
一段纯文本。换成 `"json_schema"`（Ollama 原生结构化输出 API，底层走
`/api/chat` 请求体里的 `format` 参数而不是工具调用协议）后稳定可靠，
这是最终采用的方式。这提示了一个容易被忽略的点：LangChain 同一个接口
在不同底层实现路径下，对同一个本地小模型的可靠性可能差异很大，不能默认
"官方推荐的方式"就是"这个模型上最稳的方式"，需要针对实际用的模型实测
验证。

**容错设计**：`Butler.chat()` 用 try/except 包裹结构化调用，失败时（模型
不支持结构化输出、网络错误、schema 校验失败等）退化为原有的 `backend.chat()`
纯文本调用，跳过本轮提取但保证对话不中断。这不是新增风险，而是把原方案
本来就有的"静默失败退化"（正则抠不到就是空字典，用户完全无感知）变成
"显式可观察"（退化时打一行诊断日志）。

**分层校验的取舍**：`_sane_due`/`_sane_priority`/`_sane_recurrence` 这几个
防御性校验方法全部保留，没有因为 schema 校验而删除。原因是两层校验职责
不同——schema 保证的是"字段类型对、取值在枚举范围内"（模型不可能给
`priority=9`），但保证不了"这个 `due_at` 语义上说得通"（模型完全可能给出
一个格式合法、取值也不越界，但年份是编的、早已过期的 ISO 时间字符串）。
schema 校验和语义校验是互补的两条防线，不能用其中一条替代另一条。

---

## 4. 技术栈

| 层 | 技术选型 | 选型理由 |
|---|---|---|
| Web 框架 | FastAPI + uvicorn | 类型标注即校验（Pydantic），自动生成 `/docs`，异步友好 |
| 本地推理 | Ollama（Qwen2.5-14B/7B 对话、nomic-embed-text 嵌入、MiniCPM-V 视觉） | 本地免费、离线可用、显存可控；`/api/tags` 探活即可判定可用性 |
| 云端兜底 | DeepSeek（OpenAI 兼容 `/chat/completions`） | 高压力/本地不可用时的备份链路，接口兼容意味着后续换任意 OpenAI 兼容供应商零改动 |
| 结构化提取 | LangChain（`langchain-core`/`langchain-ollama`/`langchain-openai`，锁版本） | `with_structured_output(method="json_schema")` 替代正则解析 JSON 代码块，schema 层面约束字段类型/取值范围（见 3.7） |
| 加密 | `cryptography`（Fernet + PBKDF2） | 工业标准对称加密，认证加密（防篡改）+ 密钥派生一体 |
| 密钥管理 | `keyring`（Windows Credential Locker） | 主密码不落盘明文，跨会话免重复输入 |
| 结构化存储 | SQLite（WAL） | 零运维嵌入式数据库，单文件迁移/备份简单，WAL 支持读写不互斥 |
| 向量索引 | ChromaDB 0.5.18 | 嵌入式向量库，`PersistentClient` 免额外部署；固定在 0.5.x 是为规避 1.x 的 numpy ABI 问题（见第 5 节） |
| 任务调度 | APScheduler（`BackgroundScheduler`） | 进程内后台定时任务，无需额外的消息队列/cron |
| 语音转写 | faster-whisper（本地 CPU, int8） | CTranslate2 推理后端，CPU 上也有可用速度，避免和聊天/视觉模型抢显存 |
| 图像预处理 | Pillow | 上传即压缩（限制长边像素），降低视觉模型的推理耗时和显存占用 |
| 资源监控 | psutil + nvidia-smi 子进程 | 跨平台进程级 RAM 统计 + GPU 厂商工具兜底显存统计 |
| 前端 | 原生 HTML/CSS/JS 单文件 | 零构建、零依赖，FastAPI `StaticFiles` 直接托管，改动即生效不用重新编译 |
| 测试 | 自建轻量断言框架 + 子进程隔离 | 见第 6 节 |

---

## 5. 关键技术难点与解决过程

以下是开发过程中遇到的、有一定深度的技术问题，记录问题现象、根因定位
过程和最终修复方案。

### 5.1 chromadb 段错误：Rust 扩展与 numpy ABI 不兼容

**现象**：写入一条情景记忆的向量索引时，Python 进程直接崩溃退出
（exit code 139 / SIGSEGV），`try/except` 完全拦不住。

**根因定位过程**：这类崩溃无法通过 Python 异常栈定位，需要从"哪一层
代码在跨语言边界"入手排查。`chromadb 1.5.x` 的 `collection.add()`
底层调用的是用 Rust 编译的扩展模块（`chromadb_rust_bindings`），这类
扩展在编译时会针对某个具体的 numpy C ABI 版本生成二进制接口；而项目
运行环境的 numpy 是 1.23.5（旧的 1.x 系列）。numpy 1.x 与 2.x 的数组
内存布局（`PyArrayObject` 结构体的字段排布）不兼容，Rust 扩展按照
它编译时假设的 2.x 布局去解析一个实际是 1.x 布局的内存块，直接读到了
错误的内存地址——这是原生代码层面的内存违规访问，Python 的异常处理
机制无法拦截，因为它根本不经过 Python 的异常传播路径。

这个 bug 长期潜伏未被发现的原因也值得记录：项目早期没有拉取
`nomic-embed-text` 模型，所以每次调用 `registry.embed()` 都会因为
模型不存在而抛 Python 异常，而调用处用 `except Exception: pass` 吞掉
了这个异常（设计意图是"向量写入失败不影响真相源"），导致 `collection.add()`
这一行代码从未被真正执行到。补齐嵌入模型后，这条路径第一次被打通，
段错误才第一次暴露。

**修复**：降级到 `chromadb==0.5.18`——这个版本线用纯 C++ 编译的
`chroma_hnswlib` 做向量索引，不是 Rust 绑定，其发布声明的兼容范围
本身就是 `numpy<2.0.0`，与运行环境匹配。降级过程中又牵出第二个问题：
`chromadb 0.5.18` 的 `CollectionCommon` 类在**类定义时**（即 `import
chromadb` 的瞬间，不是运行时）就会实例化一个默认的 ONNX embedding
函数作为类属性默认值，若 `onnxruntime` 因为 DLL 加载失败而导入报错，
连 `import chromadb` 本身都会失败——即便项目从未真正用到这个默认
embedding 函数（一直显式传入 Ollama 计算好的向量）。这提示了一个
排查陷阱：报错栈看起来像"依赖缺失"，但真正原因是"某个从未被调用的
默认参数在类定义时被提前构造"。修复是锁定 `onnxruntime==1.17.3`，
保证 DLL 能正常加载。

修复验证不是"跑起来不报错"就算完：专门写了隔离测试验证`add_episode()`
真实写入 + `retrieve()` 语义检索能否正确区分相关与不相关的记忆
（用"苹果"相关的记忆能否排除"天气"记忆干扰），确认语义检索功能本身
是正确的，不只是不崩溃。

**关联的环境风险**：修复过程中，`pip install onnxruntime==1.17.3`
一度触发依赖解析把共享 Anaconda 基础环境里的 numpy 从 1.23.5 静默
升级到 2.4.6，这会立刻打破同一环境下的 scipy/matplotlib/astropy/numba
等库（这些库对 numpy 主版本也有 ABI 依赖，升级同样会段错误或报错）。
这类问题只能靠**认真读 pip 命令自身的输出**——pip 在依赖冲突时会明确
打印类似 "X requires numpy<2, but you have numpy 2.4.6" 的警告，
在这一步就应该停下核实，而不是把"pip install 退出码为 0"当作安装
成功的唯一信号。修复是显式回退 `numpy==1.23.5`，再逐个验证受影响
的库能正常 `import`。

### 5.2 时间幻觉：LLM 编造年份导致核心提醒功能失效

**现象**：让模型记一个"明天下午三点"的任务，落库的 `due_at` 是一个
2023 年的日期——远早于系统当前的 2026 年。

**根因**：system prompt 里从未把"现在是什么时间"告诉模型，模型只能
用训练数据里见过的某个年份做相对时间换算的基准。这个 bug 的影响面
比表面看起来大得多：所有从对话里提取的、带相对时间描述的任务，
截止时间都会落在过去，提醒引擎会把它们**全部**判定为"已逾期"，
反复触发追问——核心的提醒/监督功能名义上在运行，实际上完全失真。

**修复**：两层防御。一是在每次对话的 system prompt 里显式注入当前
时间（含时区和星期），并在提取指令里要求模型必须以此为基准换算、
带同样的时区偏移，说不准就填 null 不要编。二是防御性兜底
`_sane_due()`：无论 prompt 层面做得多好，小模型仍有概率出错，所以
在写入数据库前再做一层校验——解析失败或者时间明显早于"现在减一天"
的一律降级为"无截止时间"。这里的取舍是：宁可损失一个截止时间提示
（用户仍能看到这条待办，只是没有到期提醒），也不能让一个虚假的
过期时间触发提醒引擎无休止地追问一个其实还没到期的任务。

单点 prompt 修复不够，必须有独立于模型输出质量的兜底校验——这是本
项目里"LLM 输出不可信、必须校验"这条原则的一个具体案例。

### 5.3 Windows 文件锁与跨组件生命周期管理

**现象**：`vector_enabled` 重新打开后，备份恢复测试在 `shutil.rmtree`
删除向量目录时抛 `PermissionError: [WinError 32] 另一个程序正在
使用此文件`。

**根因**：`Memory.close()` 只关闭了裸 SQLite 连接，从未关闭过
chromadb 的 `PersistentClient`；而 `PersistentClient` 内部自己维护
一个独立的 `chroma.sqlite3` 连接（用于存储向量索引的元数据），这个
连接不会随外层对象被垃圾回收就自动释放——Windows 的文件锁语义比
Linux 严格得多，一个进程持有打开的文件句柄，另一个操作（哪�里是同
进程内的 `rmtree`）尝试删除该文件所在目录就会直接报错，而不是像
Linux 那样允许"删除一个仍被打开的文件"。

**排查方法**：通过直接内省 `PersistentClient._system._instances`
发现 chromadb 内部是一套有统一生命周期管理的组件系统（`SqliteDB`、
`LocalSegmentManager`、`Posthog` 等子组件各自持有资源，都实现了
`.stop()`），`System.stop()` 会级联关闭所有子组件。用一个独立的
探针脚本验证：写入数据 → 不调用 `stop()` 直接 `rmtree` 会失败；
调用 `_system.stop()` 后再 `rmtree` 则成功——先用最小可复现脚本
验证根因，再改动生产代码，避免"改了但不知道到底是不是这个原因"。

**修复**：`Memory.close()` 补上 `self._client._system.stop()`。
这类问题的通用教训是：任何"看起来是纯内存对象"的第三方客户端，
只要它内部会打开文件/网络连接，就要假设它有自己的关闭协议，不能
假设 Python 的垃圾回收会替你处理好资源释放的时机与顺序。

### 5.4 自发现的回归：条件分支里被忽略的初始化路径

在完成上述修复后的代码审查中（非用户报告），发现刚加的 `close()`
逻辑里 `self._client` 只在 `_init_vector()` 的成功分支里被赋值——
如果 `vector_enabled: false`（一个完全合法、被大量支持的配置）或者
向量库初始化失败，`_client` 属性从未被创建，调用 `close()` 就会
抛 `AttributeError`。这类 bug 的特点是"新代码只在开发时验证过的
那条路径下测试过，没有覆盖到另一个同样合法但没被走到的分支"，修复
是在 `__init__` 顶部统一预置 `self._client = None`，让"属性存在但
为 None"成为所有分支下都成立的不变量。这提示了一个可以内建到代码
风格里的习惯：所有可能被条件性赋值的实例属性，都应该在 `__init__`
里先给一个默认值，而不是依赖某个分支一定会执行。

### 5.5 引入 LangChain 时的依赖冲突：共享 Anaconda 环境的连带升级

**现象**：`pip install langchain-core langchain-ollama langchain-openai` 执行
后退出码是 0（看起来成功），但 pip 在输出末尾打印了一行容易被忽略的警告：
`datasets 2.12.0 requires huggingface-hub<1.0.0, but you have
huggingface-hub 1.24.0 which is incompatible`。

**根因**：`langchain-openai` 依赖的 `openai`/`langsmith` 这条链路间接把共享
Anaconda base 环境里的 `huggingface-hub` 从 0.x 升级到了 1.24.0，而环境里
另一个不相关的包 `datasets 2.12.0` 锁定要求 `huggingface-hub<1.0.0`。验证
`import datasets` 确实报错（`ImportError: cannot import name 'HfFolder'`，
1.x 版本移除了这个符号）。

**处理方式**：这与第 5.1 节 numpy 被 pip 静默升级到 2.4.6 是同一类风险
（"pip exit code 0 不代表环境没被连带破坏"），但处理方式不同——上次是
必须修复（numpy 被破坏会直接段错误影响本项目核心功能），这次先确认
`datasets` 这个包**不在本项目的依赖图里**（`grep` 全项目代码确认没有
任何 `import datasets`），也确认 `faster-whisper`（本项目真正依赖、且
底层也用到 `huggingface_hub` 生态）在新版本下 `import` 正常。结论是
放着不处理：为一个项目不需要的包去反向锁定 `huggingface-hub<1.0.0`，
只会把刚装好的 `langchain-openai`/`langsmith` 又打破，属于用一个不存在
的问题去制造一个真实的问题。**教训**：看到 pip 冲突警告先判断"冲突的
包是不是我依赖图里的"，不是所有 pip 警告都要修，但必须先验证过才能
放着不管，不能因为"看起来能 import"就假设没事。

---

## 6. 测试策略

### 6.1 隔离原则

所有测试严禁接触真实的 `data/` 目录——这条约束来自一次真实的事故：
早期一次冒烟测试直接调用了 `create_app()`，没有做路径隔离，实际连接
到了生产的 `memory.db`。虽然核实后确认没有写入任何数据（触发的接口
都是只读或提前因参数校验失败短路），但这次教训确立了硬性约定：任何
API/Butler 级别的测试，必须先把 `Config` 的 `paths.*` 显式重定向到
一次性临时目录，且用一次性的 `BUTLER_MASTER_PASSWORD` 环境变量绕开
交互式密码输入（否则非交互 shell 会卡死在 `getpass` 等待永远不会
到来的输入）。

`tests/isolation.py` 把这套隔离逻辑封装成共享夹具：正则替换
`config.yaml` 里所有 `paths.*` 指向临时目录、设置一次性密码、把
`src.config._instance` 替换成指向临时配置的实例，全部在任何
`src.*` 模块被 import 之前完成。

### 6.2 子进程隔离运行

每个测试文件在独立子进程中运行（`tests/run_all.py`），而不是在同一
进程里依次 import 执行——因为项目里有多处全局单例（`Config._instance`、
`Cipher._instance`、一个 SQLite 连接、一个 Chroma 客户端），同进程
跑多个测试文件会互相污染单例状态，且"同一进程内创建两个 `Memory`
实例会导致 Chroma 客户端冲突崩溃"这条硬约束决定了测试之间必须有
干净的进程边界。

### 6.3 测试分层

- `test_migrate.py`：构造一个货真价实的旧版本库（只执行历史迁移，
  插入真实数据），验证升级到最新版本后原有数据逐字节完好、新增列
  对旧数据是安全默认值、迁移可重复执行不出错。
- `test_backup.py`：验证 WAL 模式下备份 API 确实捕获了未 checkpoint
  的事务（不是简单验证"文件存在"）。
- `test_reminder2.py`：用可控的假时间戳模拟"5 次连续 tick"，验证
  退避阶梯确实生效、画像记账时机确实在完成任务而非发提醒时。
- `test_smoke.py`：端到端 HTTP 层冒烟，覆盖全部 REST 端点，包括
  显式验证"静态文件挂载没有遮蔽 API 路由"这种容易被后续改动破坏
  的隐式约束。
- `test_due.py`：真实调用本地大模型验证时间提取的准确性（依赖真实
  Ollama 模型，`--fast` 模式下跳过）。

### 6.4 何时用真实断言而非"跑起来不报错"

多次 bug 修复都特别验证了"功能语义正确"而不只是"不崩溃"——例如向量
检索修复后专门验证语义相关性排序正确，而不只是 `add()` 不再段错误；
拖延分修复后验证具体的数值（"1 次逾期 + 1 次按时 → 0.50"），而不只是
"能调用 `record_reliable` 不报错"。这是因为很多这类 bug 的共同特征是
"代码能跑，输出看起来合理，但语义是错的"（比如提前量数字始终停在
某个固定值，因为分数从未真正变化过）。

---

## 7. 已知限制与后续方向

- 向量检索目前是精确的 kNN，没有做近似索引（数据量小，暂不需要）。
- 重复任务的月份步长用固定 30 天近似，不是严格的"下个月同一天"，
  对日常提醒场景够用，但严格的日历语义（如 2 月 28 日 + 1 月）
  未处理。
- 提醒引擎的语气文案生成依赖 LLM，模型不可用时有固定模板兜底，
  但兜底文案的丰富度有限。
- 尚未做多用户支持——主密码、加密密钥、画像均是单用户单例设计，
  多用户需要重新设计密钥隔离与数据分区。
- 尚未做进程守护/开机自启，目前依赖手动执行 `start.bat` 或
  `python run.py serve`。
